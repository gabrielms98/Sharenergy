import styled from 'styled-components'

export const IconMarker = styled.div`

`

export const Svg = styled.svg`
    transform: scale(0.9);
    cursor: pointer;

    &:hover {
        transform: scale(1);
    }
`